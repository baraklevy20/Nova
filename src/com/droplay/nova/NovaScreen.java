package com.droplay.nova;

// Just an abstract screen for easier use.
// Classes that use this class: DynamicScreen and StaticScreen
public abstract class NovaScreen {
//	protected final int SCREEN_WIDTH, SCREEN_HEIGHT;
	
	public NovaScreen(NovaContext context) {
//		this.SCREEN_WIDTH = (int)context.getGraphics().getWidth();
//		this.SCREEN_HEIGHT = (int)context.getGraphics().getHeight();
	}//
}
